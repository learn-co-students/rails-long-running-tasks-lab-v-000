<h1> Songs</h1>
<%= form_tag customers_upload_path, mulipart: true do %>
 <%= file_field_tag :songs%>
  <%= submit_tag "Import songs"%>
<%end%>

<ul>
<% @songs.each do |song| %>
  <li><%= display_artist(song) %> - <%= link_to song.title, song_path(song) %></li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
