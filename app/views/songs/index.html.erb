<%= form_tag songs_upload_path, multipart: true do %>
  <%= file_field_tag :file, id:'file' %><br><br>
  <%= submit_tag "Import Songs" %>
<% end %><br>
<%= button_to "Add New Song", new_song_path %>
<ul>
<% @songs.each do |song| %>
  <li><%= display_artist(song) %> - <%= link_to song.title, song_path(song) %></li>
<% end %>
</ul>
<br>
