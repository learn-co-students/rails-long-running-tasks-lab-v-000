<h1>
<% if @song.artist.nil? %>
<%= link_to @song.title, "#" %>
<h2><%= link_to "Add Artist", edit_song_path(@song) %>
<% else %>
<%= link_to @song.title, song_path(@song) %>
<% end %>
</h1>
<h3> by <%= display_artist(@song) %></h3>

<%= link_to "All Songs", songs_path %>
